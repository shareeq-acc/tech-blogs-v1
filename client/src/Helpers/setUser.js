// import store from "../Redux-Toolkit/app/store.js"
// import { loginUser } from "../Redux-Toolkit/features/User/userSlice.js";

// Login User 
const setUser = (data) => {
    // store.dispatch(loginUser(data));
}

export default setUser